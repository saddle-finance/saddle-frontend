import React from "react"
import { TextField } from "@mui/material"

export default function SwapTokenInput(): React.ReactElement {
  return (
    <div>
      <TextField name="swapTokenInput" />
    </div>
  )
}
