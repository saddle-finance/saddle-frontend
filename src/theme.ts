import { createTheme } from "@mui/material"

export const lightTheme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#F00",
    },
    secondary: {
      main: "#f50057",
    },
    background: {
      default: "#faf3ce",
    },
  },
})
